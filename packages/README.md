# SLAOPs Packages

This directory contains the packages for the SLAOps platform. Packages are shared between the applications and the core libraries. Some packages are private and not published to npm, some are public and can be published to npm to be used by customers.