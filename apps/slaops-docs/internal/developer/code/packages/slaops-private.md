# SLAOps Private

The private library for the SLAOps platform. Minimal dependencies, focused on the core domain of the SLAOps platform. External dependencies are limited to OpenAPI types and JSON Schema.

Code should only be in this package if needs to be shared with internal applications or modules and be reusable.
