# Public Content

All content here is open — no authentication required.

## `docs/` — User-facing platform docs

Sidebar auto-generated from the filesystem. Key files:

- `intro.md` — Platform overview
- `getting-started.md` — Install CLI, connect relay, run first test
- `glossary.md` — Domain term definitions (consult before using platform terms in code/docs)
- `quickstart/` — Step-by-step guided setup

## `security/` — Customer-facing security posture

Add docs here that customers read during vendor evaluation. Keep sensitive details (threat models, pen-test reports, incident runbooks) in `internal/security/` instead.
